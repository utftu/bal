{
  "name": "bal",
  "main": "./dist/bal.js",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "build-file-provider": "bun build ./src/file-provider.ts --target node --outdir ./dist",
    "build-bal": "bun build ./src/bal.ts --outdir ./dist --env=disable",
    "build": "bun run build-bal && bun run build-file-provider && bun run types",
    "types": "tsc --project tsconfig.types.json"
  },
  "files": [
    "./dist"
  ],
  "exports": {
    ".": {
      "import": "./dist/bal.js",
      "types": "./dist/types/bal.d.ts"
    },
    "./file-provider": {
      "import": "./dist/file-provider.js",
      "types": "./dist/types/file-provider.d.ts"
    }
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  }
}
